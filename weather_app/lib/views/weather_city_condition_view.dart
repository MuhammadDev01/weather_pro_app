import 'package:flutter/material.dart';

class WeatherCityConditionView extends StatelessWidget {
  const WeatherCityConditionView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}